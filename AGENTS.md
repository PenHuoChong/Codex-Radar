# 项目前置提示词

处理额度查询时：

- 查询结果可用的第一刻，立即展示“5 小时”和“每周”的美元额度；只要有有效数据就显示，不等待查询结束，也不等待人工确认。
- 美元反推必须同时显示实际校准起止时间及百分比区间，使用冻结证据端点，不用主测量起点或界面当前百分比替代；沿用旧值也保留旧证据区间。1%参考单独标注参考成本的主测量时间范围，缺失端点显示未提供。
- 刷新时保留同一账号、同一计划、同一重置周期的上一次有效估计，直到有新证据覆盖；普通模式默认保持现有策略不变。
- 索引同步失败不得清空有效额度或中断主测量；5小时与周窗口的诊断归属分别校验，单个窗口异常不得清空另一个窗口。未生成估算时保留当前结果的具体诊断，不能因为金额为空而抹掉拒绝原因。重复确认相同计价模式只重绘，不清空或重算。
- 周额度使用用户指定的1%参考策略：本次已有正的可计算美元参考消耗，且有效起止快照明确证明增量在0至不足1个百分点之间时，才可显示本次消耗×100（按1%折算）的周总额参考值。增量未知绝不当成不足1%，已达到1个百分点也不得回退到1%；保留同周期有效估算，否则明确等待同边界校准，不将主测量成本与不匹配的百分比相除。新启动界面使用套餐折算成本，未启用新口径的旧调用保持API成本；单独存储并标明口径，不冒充同边界校准证据，不用于伪造百分比、重置时间或已用/剩余美元。计价部分完整时注明“部分”。迟到的其他账号结果不得进入当前参考值。
- 周卡独立显示本次起点百分比、终点百分比和增量（如16%→18%，区间2%），即使校准成本被拒绝也保留已验证的百分比区间；它不等于严格计价区间。结果的起点字段为空时仅可回退到同账号冻结基线，不能用实时快照补起点或越过显式冲突。重置周期按规范化标识比较，不按时区字符串比较。
- 周额度完整校准采用固定锚点及用户确认的滞后终点：从36%开始，到37%用36%→37%首次校准；随后固定37%为起点，到38%用37%→38%除以1%，到39%仍用37%→38%除以1%，到40%用37%→39%除以2%。锚点后终点上限为 min(当前百分比, max(锚点+1, 当前百分比-1))，只选择上限以内实际存在的最大百分比的首次快照，不补造整数或小数快照；没有可用后续端点时保留首次校准。5小时仍采用原有不滞后的固定锚点规则。成本及分母必须来自同一冻结快照区间，主测量仍统计完整测量范围；同周期有效美元持续显示，缺少证据时不得捏造金额或回退为累计成本×100。
- 主测量起点与额度校准周期相互独立；计划、额度池或重置周期变化后，只能用当前周期的完整步长重新校准，不沿用旧周期美元，也不能被旧测量起点永久阻挡。计划类型不能冒充账号标识。
- 常规5小时/周额度卡绑定 `codex` 额度池；先选额度池再选套餐，不让模型专用池的较晚记录覆盖常规窗口。常规池缺少窗口时显示暂无，绝不从其他池补位；只有候选全部不含额度池标识时保留旧格式兼容，不与显式池混算。
- 自动套餐显示使用已接受且未过期的常规窗口计划，保留日志标识；冲突、未识别及 Pro 倍率未知必须如实说明，不猜 5x/20x。官方套餐是否通常有5小时限制，不能替代日志中的实际窗口证据，也不能触发认证读取或联网额度查询。
- 手动套餐始终提供七类及已观察的原始标识。selected_plan_missing 表示缺少所选套餐窗口，不显示为来源冲突；保留内部拒绝不匹配证据的保护。账号无5小时限制可人工设为不适用，只改变5小时显示，账号标签变化时清除，不全局禁用所有Pro的5小时窗口。
- 重复确认同一套餐或仅切换5小时“不适用”只重绘界面，不清空周额度、校准证据、缓存、测量起点或冻结终点，也不启动后台重算；真实套餐变化仍按原有规则使不兼容证据失效。
- 额度成本先完成父子规范事件去重，再检查周期归属；未知归属不得静默排除后生成完整估算。每个窗口必须给出明确诊断，并区分“本次更新”和“沿用上次结果”。
- 没有窗口或证据时不伪造百分比或严格额度；周美元可按上述用户指定公式显示明确标记的参考值。没有正的可计算美元成本时不可捏造金额，应说明原因。
- 缺失、空值、非有限或越界百分比不是0%；被拒绝的旧快照及其套餐选择原始候选不得回流。额度证据必须保持原冻结端点，不能为了匹配界面新快照而替换时间；旧值保留应以界面已接受的同周期窗口为准。
- 界面先接受较新快照时，不得仅因观察时间不同就丢弃迟到的有效冻结校准：完整账号/计划/常规额度池/窗口/重置标识一致且百分比不倒退时，可显示该冻结校准及其截至时间，等待新边界复核；不能重绑端点或覆盖已有更晚校准。已观察到完整周增量后，暂缺快照或无效成本不能重新打开1%兜底。
- 同一重置周期并存不同计划的快照时，不得将“时间最新”当作当前账号归属证明；应保留并展示冲突来源，不硬编码优先某个计划。相同校准边界与成本的重复查询标为“沿用上次结果”，展示校准截至时间；百分比乘总额得到的美元必须标为“比例外推”，不能冒充实际消耗。
- 用户确认升级套餐后，可通过“当前套餐”明确选择日志计划，仅对额度快照与校准应用该选择；主测量累计不受影响。选择不证明账号身份，账号标签变化时清除；未找到所选计划时不得回退到其他计划的百分比或美元。
- 不得牺牲快照同边界、父子去重或不读取 auth/私人数据的约束；访问任何私人数据前必须取得明确授权。
- 修改后执行合成回归，覆盖有效数据即时显示、刷新保留旧估计、不可估不填 0，以及上述边界约束。
- 时间段结果的显示或额度诊断失败不得终止正在进行的测量；保留原始起点和有效结果，允许重试。最终结算显示失败时保留冻结结束边界，不能重新捕获结束时间。对过滤、去重后的集合先用数组包装，再读取 Count，兼容 PowerShell 5.1。
- 周期用量的启动、显示、失败和停止回调只允许恢复自身任务状态，不能推进主测量 Generation 或清空其起止边界；超时取消时必须等旧线程停止再允许同类新任务。
- 周美元参考值的账号校验必须先于任何状态写入，包括零成本、缺字段或异常数值的清空分支。缓存读取别名按首个有效非负整数取值，显式0有效，空值/异常值继续回退；完整JSON、快速解析和索引器必须一致。
- 用户已选择订阅套餐折算口径：新启动主测量的5小时/周额度使用“普通API估值×套餐模式倍率”的独立美元参考（GPT-6/5.6/5.5 Fast 2.5×，GPT-5.4 Fast 2×，仅限价格表明确模型）。主API美元、Token及服务端百分比不变；参考不含缓存写入溢价，长上下文API估值仅是参考单位，不能声称为官方订阅倍率或账单。1%周参考同样使用套餐折算成本×100并独立标注。旧调用未显式启用新口径时保持API语义，不能影响正在运行的旧窗口；严禁将两种口径的缓存或估算混用。
